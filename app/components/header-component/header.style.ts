import { StyleSheet, ViewStyle } from "react-native";


interface Styles{
    menu: ViewStyle,
    
}

export const headersStyle = StyleSheet.create<Styles>({
    menu:{
        color: "white"
    }

})